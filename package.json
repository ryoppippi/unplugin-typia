{
  "name": "unplugin-typia-monorepo",
  "type": "module",
  "description": "",
  "author": "ryoppippi",
  "license": "MIT",
  "scripts": {
    "build": "bun run --filter '*' build",
    "lint": "bun run --filter '*' lint",
    "format": "bun run --filter '*' format",
    "test": "bun run --filter '*' test",
    "deploy": "bun run --filter '*' deploy",
    "prepare": "bun x bunpare",
    "taze": "bun x taze -r && bun i"
  },
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "trustedDependencies": [
    "@sveltejs/kit",
    "esbuild",
    "nextjs",
    "svelte-preprocess",
    "sveltekit",
    "vite-hono",
    "vite-react",
    "vite-vanilla",
    "workerd"
  ],
  "packageManager": "bun@1.1.24",
  "private": true
}
